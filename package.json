{
  "name": "codebouncer",
  "version": "0.1.0",
  "private": true,
  "description": "CodeBouncer - a real-time guardrail that intercepts AI-suggested packages and blocks hallucinated, typosquatted, or malicious dependencies before they are installed.",
  "scripts": {
    "install:all": "npm install && npm --prefix server install && npm --prefix client install",
    "dev": "concurrently -n server,client -c green,cyan \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix client run build",
    "deploy:build": "npm run install:all && npm run build",
    "start": "npm --prefix server start"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
